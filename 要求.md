# QQ / 微信动图表情包生成器 - 智能体 

## 一、项目概述

本项目基于 Python + LangChain 智能体架构，封装一系列图像处理 Skill 工具，实现「图片上传 → 智能处理 → 动图生成 → 平台适配」全流程表情包自动化制作。智能体可根据用户自然语言指令，自动调度对应 Skill 完成任务。

---

## 二、阶段一：环境准备与依赖安装

### 2.1 阶段目标

搭建开发运行环境，安装所有必需的依赖库。

### 2.2 功能清单

- Python 基础运行环境
- 图像处理库（PIL、OpenCV、imageio）
- 智能体框架（LangChain）
- 抠图引擎（rembg）
- Web 服务框架（FastAPI）

### 2.3 执行命令

```
pip install langchain pillow imageio opencv-python rembg fastapi python-multipart numpy uvicorn
```

---

## 三、阶段二：核心 Skill 功能模块开发

### 模块 1：全局配置与基础类

#### 功能说明

定义统一的输出目录、微信 / QQ 表情包标准尺寸参数，作为所有 Skill 的基础配置。

#### Skill 实现代码

```
from PIL import Image, ImageFilter, ImageDraw
import imageio
import cv2
import numpy as np
from rembg import remove
import os

# 平台标准尺寸配置
WECHAT_EMOJI_SIZE = (240, 240)  # 微信表情包最佳尺寸
QQ_EMOJI_SIZE = (320, 320)      # QQ表情包最佳尺寸
SAVE_DIR = "./emoji_output"     # 输出目录
os.makedirs(SAVE_DIR, exist_ok=True)
```

---

### 模块 2：背景抠图 Skill

#### 功能说明

上传任意图片，自动移除背景，生成透明底 PNG 图片，是制作表情包的基础步骤。

#### 对应 Skill 代码

```
from langchain.tools import tool

class EmojiMakerSkill:
    """表情包&动图生成智能体专属Skill集合"""

    @tool("图片背景抠图技能")
    def remove_bg(self, img_path: str, save_path: str = None) -> str:
        """
        技能：上传图片自动抠图，生成透明底表情图
        :param img_path: 本地输入图片路径
        :param save_path: 抠图后保存路径（可选）
        :return: 处理完成的图片路径
        """
        input_img = Image.open(img_path).convert("RGBA")
        out_img = remove(input_img)
        if not save_path:
            save_path = f"{SAVE_DIR}/no_bg_emoji.png"
        out_img.save(save_path)
        return f"抠图完成，保存路径：{save_path}"
```

---

### 模块 3：尺寸适配 Skill

#### 功能说明

按照微信或 QQ 平台的标准表情包尺寸进行等比缩放，确保发送后显示正常。

#### 对应 Skill 代码

```
    @tool("表情尺寸适配技能")
    def resize_emoji(self, img_path: str, platform: str = "wechat") -> str:
        """
        技能：适配微信/QQ标准表情包尺寸
        :param img_path: 输入图片路径
        :param platform: 目标平台，可选 wechat / qq
        :return: 处理后图片路径
        """
        img = Image.open(img_path)
        target_size = WECHAT_EMOJI_SIZE if platform == "wechat" else QQ_EMOJI_SIZE
        img = img.resize(target_size, Image.Resampling.LANCZOS)
        save_p = f"{SAVE_DIR}/{platform}_size_emoji.png"
        img.save(save_p)
        return f"{platform}尺寸适配完成，路径：{save_p}"
```

---

### 模块 4：静态表情美化 Skill

#### 功能说明

对表情包进行亮度提升、双边滤波磨皮美化，提升视觉效果。

#### 对应 Skill 代码

```
    @tool("静态表情美化技能")
    def beauty_emoji(self, img_path: str, bright: int = 10, smooth: bool = True) -> str:
        """
        技能：提亮、磨皮美化静态表情包
        :param img_path: 输入图片路径
        :param bright: 亮度提升值（0-50）
        :param smooth: 是否开启磨皮
        :return: 处理后图片路径
        """
        img = cv2.imread(img_path)
        # 亮度调整
        hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)
        h, s, v = cv2.split(hsv)
        lim = 255 - bright
        v[v > lim] = 255
        v[v <= lim] += bright
        final_hsv = cv2.merge((h, s, v))
        res = cv2.cvtColor(final_hsv, cv2.COLOR_HSV2BGR)
        # 双边滤波磨皮
        if smooth:
            res = cv2.bilateralFilter(res, 5, 50, 50)
        save_p = f"{SAVE_DIR}/beauty_emoji.png"
        cv2.imwrite(save_p, res)
        return f"表情美化完成，路径：{save_p}"
```

---

### 模块 5：多图合成 GIF 动图 Skill

#### 功能说明

将多张静态图片按顺序合成为 GIF 动图，支持自定义帧间隔和循环次数。

#### 对应 Skill 代码

```
    @tool("多图合成GIF动图技能")
    def make_gif_emoji(self, img_list: list, duration: float = 0.3, loop: int = 0) -> str:
        """
        技能：多张图片合成微信QQ通用GIF动图表情包
        :param img_list: 图片路径列表，按帧顺序排列
        :param duration: 单帧时长，单位秒
        :param loop: 循环次数，0表示无限循环
        :return: 生成的GIF路径
        """
        frames = []
        for p in img_list:
            frame = Image.open(p)
            frames.append(frame)
        gif_path = f"{SAVE_DIR}/dynamic_emoji.gif"
        frames[0].save(
            gif_path,
            save_all=True,
            append_images=frames[1:],
            duration=duration * 1000,
            loop=loop
        )
        return f"GIF动图表情包生成成功，路径：{gif_path}"
```

---

### 模块 6：单图抖动特效 Skill

#### 功能说明

仅需一张图片，自动生成随机偏移的抖动 / 摇摆动态效果，是爆款表情包常用特效。

#### 对应 Skill 代码

```
    @tool("单图逐帧抖动动图技能")
    def shake_gif_from_single(self, img_path: str, frame_num: int = 6) -> str:
        """
        技能：单张图片自动生成抖动摇摆动态表情包
        :param img_path: 输入单张图片路径
        :param frame_num: 生成帧数
        :return: 抖动GIF路径
        """
        base_img = Image.open(img_path).convert("RGBA")
        w, h = base_img.size
        shake_frames = []
        offset_range = 8
        for i in range(frame_num):
            dx = np.random.randint(-offset_range, offset_range)
            dy = np.random.randint(-offset_range, offset_range)
            new_img = Image.new("RGBA", (w, h), (0, 0, 0, 0))
            new_img.paste(base_img, (dx, dy))
            shake_frames.append(new_img)
        out_gif = f"{SAVE_DIR}/shake_dynamic.gif"
        shake_frames[0].save(
            out_gif,
            save_all=True,
            append_images=shake_frames[1:],
            duration=200,
            loop=0
        )
        return f"单图抖动动图生成完成，路径：{out_gif}"
```

---

### 模块 7：GIF 压缩适配 Skill

#### 功能说明

压缩 GIF 文件体积，满足微信表情包单张不超过 1MB 的限制，确保可正常发送。

#### 对应 Skill 代码

```
    @tool("动图压缩适配微信发送")
    def compress_gif(self, gif_path: str, max_size_kb: int = 500) -> str:
        """
        技能：压缩GIF体积，适配微信表情包大小限制
        :param gif_path: 输入GIF路径
        :param max_size_kb: 目标最大体积（单位KB）
        :return: 压缩后GIF路径
        """
        frames = imageio.mimread(gif_path)
        new_frames = [cv2.resize(f, (200, 200)) for f in frames]
        compress_path = f"{SAVE_DIR}/compress_wechat.gif"
        imageio.mimsave(compress_path, new_frames, duration=0.25)
        return f"微信适配压缩动图完成，路径：{compress_path}"
```

---

## 四、阶段三：智能体调度集成

### 4.1 阶段目标

将所有 Skill 注册到 LangChain 智能体中，实现自然语言指令自动调度工具。

### 4.2 功能说明

- 大模型解析用户自然语言需求
- 自动规划处理步骤
- 依次调用对应 Skill 工具
- 输出最终结果路径

### 4.3 智能体实现代码

```
from langchain.agents import initialize_agent, AgentType
from langchain_openai import ChatOpenAI

# 1. 初始化大模型（可替换为本地模型）
llm = ChatOpenAI(model="gpt-3.5-turbo", temperature=0)

# 2. 实例化Skill集合
emoji_skill = EmojiMakerSkill()

# 3. 收集所有工具
emoji_tools = [
    emoji_skill.remove_bg,
    emoji_skill.resize_emoji,
    emoji_skill.beauty_emoji,
    emoji_skill.make_gif_emoji,
    emoji_skill.shake_gif_from_single,
    emoji_skill.compress_gif
]

# 4. 创建表情包生成专属智能体
emoji_agent = initialize_agent(
    tools=emoji_tools,
    llm=llm,
    agent=AgentType.STRUCTURED_CHAT_ZERO_SHOT_REACT_DESCRIPTION,
    verbose=True
)

# 5. 调用示例
if __name__ == "__main__":
    user_prompt = "对图片 test.jpg 抠图，适配微信尺寸，美化后生成抖动GIF并压缩"
    result = emoji_agent.run(user_prompt)
    print(result)
```

---

## 五、阶段四：Web 界面与上传接口

### 5.1 阶段目标

搭建美观易用的 Web 界面，支持图片上传与结果展示，对接智能体后端。

### 5.2 功能清单

- 图片上传接口
- 智能体自动处理
- 结果文件返回
- 前端可视化界面

### 5.3 FastAPI 后端实现

```
from fastapi import FastAPI, UploadFile, File
from fastapi.staticfiles import StaticFiles
import shutil
import os

os.makedirs("./upload", exist_ok=True)

app = FastAPI(title="微信QQ表情包动图生成器")
app.mount("/output", StaticFiles(directory=SAVE_DIR), name="output")

@app.post("/api/generate")
async def generate_emoji(file: UploadFile = File(...), platform: str = "wechat"):
    # 保存上传文件
    save_path = f"./upload/{file.filename}"
    with open(save_path, "wb") as f:
        shutil.copyfileobj(file.file, f)
    
    # 调用智能体全流程处理
    prompt = f"处理图片 {save_path}，抠图、适配{platform}尺寸、生成抖动动图并压缩"
    result = emoji_agent.run(prompt)
    
    return {
        "status": "success",
        "message": result,
        "download_url": f"/output/compress_wechat.gif"
    }

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="127.0.0.1", port=8000)
```

---

## 六、功能总览对照表

表格

| 功能模块 | 对应 Skill 名称 | 输入 | 输出 |
| --- | --- | --- | --- |
| 背景移除 | 图片背景抠图技能 | 原图路径 | 透明底 PNG |
| 尺寸适配 | 表情尺寸适配技能 | 图片路径 + 平台 | 标准尺寸图 |
| 美颜美化 | 静态表情美化技能 | 图片路径 + 参数 | 美化后图片 |
| 多图动图 | 多图合成 GIF 动图技能 | 图片路径列表 | GIF 动图 |
| 单图抖动 | 单图逐帧抖动动图技能 | 单张图片 | 抖动 GIF |
| 体积压缩 | 动图压缩适配微信发送 | GIF 路径 | 压缩后 GIF |

---

## 七、标准使用流程

1. **上传阶段**：用户通过 Web 界面或本地路径传入原始图片
2. **预处理阶段**：智能体自动执行抠图 → 尺寸适配 → 美化
3. **动图生成阶段**：根据需求选择「多图合成」或「单图抖动」生成 GIF
4. **优化输出阶段**：压缩体积，适配微信 / QQ 平台限制
5. **交付阶段**：返回最终表情包文件路径与下载地址

---

## 八、可拓展 Skill 清单

1. **文字贴纸 Skill**：在表情包上添加自定义文字气泡
2. **特效变换 Skill**：翻转、旋转、镜像、鱼眼等特效
3. **边框装饰 Skill**：添加表情包外框、圆角、阴影
4. **视频截帧 Skill**：从短视频中截取片段转 GIF
5. **批量生成 Skill**：文件夹图片批量一键转表情包

提示：以上 Skill 仅作为参考，具体实现需要根据实际需求进行扩展。